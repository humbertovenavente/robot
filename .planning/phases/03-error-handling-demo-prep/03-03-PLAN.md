---
phase: "03"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - DEMO_CHECKLIST.md
autonomous: false
requirements:
  - DEM-01
  - DEM-02
  - DEM-03

must_haves:
  truths:
    - "DEMO_CHECKLIST.md exists at repo root with checkbox items an operator can tick manually"
    - "Checklist covers: camera index / USB port, YOLO model file, lighting + QR readability, multi-station connectivity"
    - "Checklist includes a Demo Script section for DEM-01 (single-station) and DEM-02 (multi-station) validation"
    - "Estimated time per section totals <= 30 minutes"
    - "Operator has physically verified all checklist items on demo hardware"
  artifacts:
    - path: "DEMO_CHECKLIST.md"
      provides: "Pre-demo operator runbook"
      contains: "Pre-Demo Checklist"
  key_links:
    - from: "DEMO_CHECKLIST.md"
      to: "station_config.yaml"
      via: "camera_index and yolo_model_path field names referenced in checklist items"
      pattern: "camera_index"
---

<objective>
Create DEMO_CHECKLIST.md at the repo root -- the mandatory pre-demo runbook covering ports/camera, YOLO model file, lighting and QR readability, multi-station connectivity, and a demo script section for DEM-01/DEM-02 validation. Then execute the checklist on actual demo hardware before the demo session.

Purpose: DEM-03 requires a 30-minute pre-demo checklist to prevent the top demo-day failure modes (wrong camera index, missing model file, QR unreadable under venue lighting, orchestrator not reachable from station laptops).
Output: DEMO_CHECKLIST.md committed to repo; human checkpoint confirming it passes on demo hardware.
</objective>

<execution_context>
@/Users/humbertovenavente/CascadeProjects/windsurf-project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/humbertovenavente/CascadeProjects/windsurf-project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-error-handling-demo-prep/03-CONTEXT.md
@station_config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DEMO_CHECKLIST.md at repo root</name>
  <read_first>
    - .planning/phases/03-error-handling-demo-prep/03-CONTEXT.md (D-11 through D-13, Specifics section)
    - station_config.yaml (exact field names: camera_index, yolo_model_path, orchestrator_url, class_to_bin)
    - requerimientos.md section 7 RF-12
  </read_first>
  <files>DEMO_CHECKLIST.md</files>
  <action>
Create `/Users/humbertovenavente/CascadeProjects/windsurf-project/DEMO_CHECKLIST.md`.

The file must be a markdown document with GitHub-style checkboxes. Read station_config.yaml first to fill in the actual bin numbers from class_to_bin in the Demo Script section.

Required sections and checkbox items (write all of these verbatim, filling in bin numbers from station_config.yaml):

SECTION HEADER: "# Pre-Demo Checklist"
Subtitle: "**Estimated total: ~25 minutes** | **Must complete before demo starts**"
Note: "Tick each box as it passes. If any Step 1-4 item cannot be ticked, resolve before proceeding to the Demo Script."

STEP 1 "## Step 1 -- Ports and Camera Index (~5 min)" with these checkboxes:
  - "USB camera is physically connected. Run `python -c \"import cv2; cap=cv2.VideoCapture(0); print(cap.isOpened())\"` -- prints True. If False, try index 1 or 2 and update camera_index in station_config.yaml."
  - "station_config.yaml camera_index value matches the index that returned True."
  - "LEGO brick USB/Bluetooth connection is reachable. Run `ls /dev/ttyUSB*` (Linux) or `ls /dev/tty.usbmodem*` (macOS) and confirm at least one device appears."
  - "station_config.yaml robot_implementation is set to ev3 (or spike) -- not stub -- for the physical demo run."

STEP 2 "## Step 2 -- YOLO Model File (~2 min)" with these checkboxes:
  - "station_config.yaml yolo_model_path points to the trained weights file (e.g. models/best.pt)."
  - "The file exists and is non-zero bytes: `python -c \"from pathlib import Path; p=Path('models/best.pt'); print(p.exists(), p.stat().st_size)\"` -- first value True, second value > 0."
  - "Replace models/best.pt above with the actual path in yolo_model_path if different."

STEP 3 "## Step 3 -- Lighting and QR Readability (~10 min)" with these checkboxes:
  - "Start a 10-second camera preview: `python -c \"import cv2; cap=cv2.VideoCapture(0); [cv2.imshow('p',cap.read()[1]) or cv2.waitKey(1) for _ in range(300)]; cv2.destroyAllWindows(); cap.release()\"`. Preview window opens without error."
  - "Hold Package A QR in front of the camera. QR appears clearly in the preview -- no motion blur, not washed out."
  - "Repeat for Package B QR."
  - "Repeat for Package C QR."
  - "Hold the ROBOT QR (affixed to the robot arm) in front of the camera. Confirm it appears clearly."
  - "If any QR does not scan clearly, adjust lighting or reprint at higher contrast before continuing -- do NOT skip this check."
  - "Smoke-test: `python -c \"from qr import decode_from_frame; print('qr module ok')\"` -- prints qr module ok."

STEP 4 "## Step 4 -- Multi-Station Connectivity (~8 min)" with note "(Skip if running single-station mode)" and these checkboxes:
  - "Ping test: `ping -c 3 <orchestrator-ip>` from each station laptop -- 0 percent packet loss."
  - "Start orchestrator: `python orchestrator.py` -- terminal shows Uvicorn running on http://0.0.0.0:8000."
  - "Open dashboard in browser: http://[orchestrator-ip]:8000/ -- dashboard grid loads."
  - "Start Station 1 with orchestrator_url: ws://[orchestrator-ip]:8000/ws in station_config.yaml. Within 5 seconds, Station 1 card appears in the dashboard with status free."
  - "Start Station 2 on a second laptop with a different station_id. Within 5 seconds, Station 2 card appears alongside Station 1."
  - "Both station cards show free status simultaneously -- no duplicate cards, no missing cards."

DEMO SCRIPT SECTION "## Demo Script"

Subsection "### DEM-01 -- Single-Station Happy Path (~5 min, one station only)" with these checkboxes (replace BIN_A, BIN_B, BIN_C with the integer values from station_config.yaml class_to_bin mapping):
  - "Place Package A (class A) in front of the camera. Robot moves to Bin BIN_A, deposits, returns home. Dashboard shows processing then free. JSONL entry: status completed, class A."
  - "Place Package B. Robot delivers to Bin BIN_B, returns home. JSONL entry: status completed, class B."
  - "Place Package C. Robot delivers to Bin BIN_C, returns home. JSONL entry: status completed, class C."
  - "All 3 packages sorted in a single uninterrupted session -- no manual intervention, no stuck cycle lock."

Subsection "### DEM-02 -- Multi-Station Concurrent Run (>=5 min, both stations)" with these checkboxes:
  - "Both stations are running under the orchestrator (Step 4 complete)."
  - "Place packages alternately on Station 1 and Station 2 at roughly 30-second intervals for at least 5 minutes."
  - "No crash or stuck cycle lock on either station during the 5-minute window."
  - "Dashboard continues to update both cards throughout -- no stale or frozen cards."
  - "After 5 minutes: check JSONL logs on both station laptops -- each shows multiple completed entries."

OPTIONAL SECTION "## Optional" with this checkbox:
  - "Battery level checked (operator discretion -- not mandatory)."

Footer line: "*Phase 3 -- DEM-03 | Deadline: 2026-04-16*"
  </action>
  <verify>
    <automated>test -f /Users/humbertovenavente/CascadeProjects/windsurf-project/DEMO_CHECKLIST.md && grep -c "\- \[ \]" /Users/humbertovenavente/CascadeProjects/windsurf-project/DEMO_CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f DEMO_CHECKLIST.md` exits 0
    - `grep -c "- \[ \]" DEMO_CHECKLIST.md` >= 20 (at least 20 checkbox items)
    - `grep "camera_index" DEMO_CHECKLIST.md` returns at least 1 line
    - `grep "yolo_model_path" DEMO_CHECKLIST.md` returns at least 1 line
    - `grep "QR" DEMO_CHECKLIST.md` returns at least 3 lines (Package A, B, C checks)
    - `grep -i "orchestrator" DEMO_CHECKLIST.md` returns at least 1 line
    - `grep -i "Single-Station\|DEM-01" DEMO_CHECKLIST.md` returns at least 1 line
    - `grep -i "Multi-Station\|DEM-02\|5 min" DEMO_CHECKLIST.md` returns at least 1 line
    - `grep -i "battery" DEMO_CHECKLIST.md` returns at least 1 line (in Optional section only, not in Steps 1-4)
  </acceptance_criteria>
  <done>DEMO_CHECKLIST.md exists at repo root with >=20 checkbox items covering all D-12 mandatory sections plus Demo Script sections for DEM-01 and DEM-02; battery is in Optional section only</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute pre-demo checklist on demo hardware</name>
  <action>Work through DEMO_CHECKLIST.md Steps 1-4 on the actual demo hardware. Tick each checkbox only when the condition is confirmed. Resolve any failures before the demo session.</action>
  <what-built>DEMO_CHECKLIST.md -- pre-demo operator runbook covering ports/camera, YOLO model, lighting+QR, multi-station connectivity, and DEM-01/DEM-02 demo script</what-built>
  <how-to-verify>
    On actual demo-day hardware in the actual demo room (or the closest available approximation before the demo):

    1. Open DEMO_CHECKLIST.md and work through Steps 1-4 in order.
    2. For each checkbox item, perform the described action on the physical hardware.
    3. Tick the box only when the described condition is confirmed.
    4. Step 3 (QR readability) requires running the camera preview with the actual packages -- do not skip.
    5. Step 4 (multi-station) requires both station laptops and the orchestrator running simultaneously.

    After completing Steps 1-4:
    - Run the DEM-01 Demo Script with one physical station -- all 3 package classes sorted in one session.
    - If both stations are available, run the DEM-02 section for >=5 minutes.

    Success = every checkbox in Steps 1-4 is ticked with zero blocking failures.
    A "blocking failure" is any item that cannot be resolved within the demo setup window.

    If any item fails and cannot be resolved, report what failed and what the blocker is.
  </how-to-verify>
  <resume-signal>Type "checklist passed" if all Steps 1-4 items pass on demo hardware. Or describe which items failed and what the blockers are.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator to hardware | Human executes CLI commands directly on demo hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | Wrong camera_index causes cv2.VideoCapture to return isOpened()=False | mitigate | Step 1 checklist item instructs operator to try indices 0/1/2 and update station_config.yaml |
| T-03-07 | Denial of Service | Missing YOLO model file causes load_vision to fail at startup | mitigate | Step 2 checklist item verifies file exists and is non-zero bytes before demo |
| T-03-08 | Denial of Service | Orchestrator unreachable from station laptops due to network | mitigate | Step 4 ping test + dashboard load confirm connectivity before demo starts |
</threat_model>

<verification>
- `test -f DEMO_CHECKLIST.md` exits 0
- `grep -c "- \[ \]" DEMO_CHECKLIST.md` >= 20
- Human checkpoint returns "checklist passed"
</verification>

<success_criteria>
- DEMO_CHECKLIST.md exists at repo root with all D-12 mandatory sections
- Every checkbox item is specific and actionable (has a command or concrete observable)
- Steps 1-4 total <= 30 minutes
- DEM-01 and DEM-02 demo script sections present
- Human operator completes the checklist on demo hardware with zero blocking failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-handling-demo-prep/03-03-SUMMARY.md`
</output>
